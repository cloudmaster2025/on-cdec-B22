pipeline {
    agent any
    stages{
        stage ('Pull') {
            steps {
                git branch: 'branch1', url: 'https://github.com/cloudmaster2025/sonar.git'  
            }            
        } 
        stage ('Build'){
            steps{
                sh '/opt/apache-maven-3.9.9/bin/mvn clean package'
            }
        }
        stage ('Test'){
            steps{
                echo 'code tested'
            }
        }
        stage ('deploy'){
            steps{
                echo 'code deployed'
            }
        }
    }
}
